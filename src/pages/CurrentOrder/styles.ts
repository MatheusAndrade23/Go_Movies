import styled, { css } from 'styled-components';

export const CurrentOrderContainer = styled.main`
  width: 100%;
  padding: 3rem 1rem;
  margin: 0 auto;

  display: flex;
  flex-flow: row wrap;
  align-items: flex-start;
  gap: 4rem;
  justify-content: center;
`;
